<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="OpenClawTray.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenClawTray">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Hub NavigationView selection indicator: follow the user's
                 Windows accent (was hard-coded lobster red #E74C3C). Resolves
                 against {ThemeResource SystemAccentColor} so the indicator
                 updates if the user changes their accent. -->
            <SolidColorBrush x:Key="NavigationViewSelectionIndicatorForeground"
                             Color="{ThemeResource SystemAccentColor}"/>

            <!-- Primary action button: use the WinUI default accent fill
                 (driven by the user's Windows accent color), not the old
                 lobster-red brand override. Foreground and CornerRadius keep
                 the previous app-specific values so layout stays identical. -->
            <Style x:Key="AccentButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{ThemeResource AccentFillColorDefaultBrush}" />
                <Setter Property="Foreground" Value="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
